# Cat Face Tracker 😺

A Flask + OpenCV + MediaPipe web app that detects facial expressions
and shows a corresponding cat reaction.

## Run locally
pip install flask opencv-python mediapipe
python main.py
